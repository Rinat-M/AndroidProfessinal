package com.gb.stopwatch.core.provider

import android.content.Context
import com.gb.stopwatch.R

class StringsProvider(
    context: Context
) {

    val defaultTime = context.getString(R.string.default_time)

}